# 테스트 코드에서 사용하는 application.yml
# 테스트 코드 환경에서는 이 파일이 우선권을 가지고 실행된다.

# 스프링 부트는 별도의 설정이 없으면 모두 메모리 환경으로 실행된다.
# 따라서, 모든 설정을 주석 처리해도 된다.
spring:
#  datasource:
#    url: jdbc:h2:mem:test # 메모리 환경에서 h2를 띄운다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace